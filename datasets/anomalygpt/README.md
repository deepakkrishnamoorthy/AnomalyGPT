# AnomalyGPT Dataset Folder

Use one directory per video clip:

```text
video_id/
  frames/
  masks/
  tracks.json
  qa.json
```

The `_template` folder shows the expected schema and is tracked by git. Real dataset samples are ignored to avoid committing large data files.
